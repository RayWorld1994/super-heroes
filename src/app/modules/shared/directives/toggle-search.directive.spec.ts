import { ToggleSearchDirective } from './toggle-search.directive';

describe('ToggleSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSearchDirective();
    expect(directive).toBeTruthy();
  });
});
